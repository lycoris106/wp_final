"""Query"""
    type Query {
        user(username: String!, password: String!): User
    }

"""Mutation"""
    type Mutation {

    }

"""Subscription"""
    type Subscription {

    }

"""Type"""
    type User {
        id: ID
    }

"""Input"""
    input CreateUserInput {
        username: String!
        password: String!
    }

    input UpdateUserInput {
        username: String!
    }