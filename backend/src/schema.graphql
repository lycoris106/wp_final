type Recipe {
    id: ID!
    title: String!
    url: String
    ingredients: [String!]!
    instructions: [String!]!
}

type Query {
    recipes(input: QueryRecipeInput): [Recipe]!
}

input QueryRecipeInput {
    ingredients: [String]!
}