type Recipe {
    id: ID!
    title: String!
    url: String!
    ingredients: [String]
    instructions: [String]
}

type Query {
    recipes(ingredients: [String]): [Recipe]!
    recipe(id: ID!): Recipe!
}